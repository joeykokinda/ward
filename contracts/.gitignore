# Foundry build output
out/
cache/
broadcast/

# Vendored dependencies (re-install via: forge install / forge soldeer install)
lib/

# Deployments moved to the repo-root /deployments/ (single canonical location
# read by the agent + frontend). Deploy.s.sol / export-abis.sh write there now;
# nothing under contracts/ to keep. See /deployments/.
