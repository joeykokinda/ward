[project]
name = "ward-sim"
version = "1.0.0"
description = "WARD device telemetry simulator"
requires-python = ">=3.12"
dependencies = [
    "fastapi==0.136.3",
    "uvicorn==0.49.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
