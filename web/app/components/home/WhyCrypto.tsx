export function WhyCrypto() {
  return (
    <section
      id="why"
      className="ward-hatch-ink flex min-h-svh scroll-mt-16 items-center border-y border-accent-hover bg-accent text-[#0a0a0f]"
    >
      <div className="reveal mx-auto w-full max-w-5xl px-6 py-24 md:py-28">
        <div className="text-[11px] font-semibold uppercase tracking-[0.08em] text-[#0a0a0f]/70">
          Why crypto
        </div>
        <h2 className="mt-4 max-w-4xl text-5xl font-bold uppercase leading-[0.98] tracking-tight md:text-7xl">
          Software can&apos;t open a bank account.
        </h2>
        <div className="mt-8 max-w-2xl space-y-5 text-[15px] font-medium leading-relaxed text-[#0a0a0f]/80 md:text-base">
          <p>
            For a homeowner paying a local plumber, cards and ACH work fine. Crypto
            is load-bearing only when the buyer is software that can&apos;t open a
            bank account.
          </p>
          <p>
            A DePIN network paying its hardware techs, a DAO posting jobs from
            treasury, an AI agent with its own funds. On-chain settlement is the only
            option, not a gimmick.
          </p>
        </div>
      </div>
    </section>
  );
}
